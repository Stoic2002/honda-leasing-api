# EXAMPLE / TEMPLATE Configuration file for Honda Leasing API
# Duplicate this file as app.dev.yaml / app.prod.yaml to use.
app:
  port: "8080"
  env: "development"

database:
  host: "localhost"
  port: "5432"
  user: "your_db_username"
  password: "your_db_password"
  name: "your_db_name"
  sslmode: "disable"

jwt:
  secret: "your_super_secret_jwt_key"
  expire_minutes: 60
  refresh_days: 7
